For run the backEnd, open the 'BackEnd' folder and active the virtual environment with 'source/venv/scripts/activate' comand.

For run the frontEnd open the 'landingpage' folder and active the project with 'npm run serve' comand.